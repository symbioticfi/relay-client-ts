{
  "name": "@symbioticfi/relay-client-ts",
  "version": "0.1.0",
  "description": "TypeScript client for Symbiotic Relay",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src/gen"
  ],
  "scripts": {
    "update:proto": "bash ./scripts/update-proto.sh",
    "generate": "buf generate",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint . --ext .ts",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.12.0",
    "eslint": "^9.0.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/symbioticfi/relay-client-ts.git"
  },
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20"
  }
}